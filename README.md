# K-Means-Clustering-of-Iris-and-Country-Datasets

Before running the program, necessary installations must be made.

pip install pandas
pip install numpy
pip install seaborn
pip install matplotlib
pip install scikit-learn
